app = "daepamil"
primary_region = "nrt"

[build]

[env]
  DATA_DIR = "/app/data"

[http_service]
  internal_port = 8000
  force_https = true
  auto_stop_machines = "off"
  auto_start_machines = true

[[mounts]]
  source = "data"
  destination = "/app/data"

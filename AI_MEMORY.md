# CODERULE

범용 코딩 규약.
---

## §1 사고 기록

[사건 1] 무단 삭제로 인한 미커밋 작업물 소실
AI가 인코딩 오류를 자체 해결하겠다며 승인 없이 강제 삭제와 히스토리 되돌리기를 실행했다.
→ 수 시간 분량의 커밋되지 않은 작업이 영구 소실. 복구 불가.

[사건 2] 동기화 커서 초기화로 인한 데이터 파괴
AI가 연동 오류를 고치겠다며 승인 없이 동기화 상태값을 0으로 초기화하고, 수집 상한을 건 채 재실행했다.
→ 과거 데이터를 다시 가져오지 못한 채 최신 일부만 덮어써서 두 달치 운영 데이터가 소멸.

- 두 사건 모두 AI의 의도는 선했고, 판단은 그럴듯했으며, 결과는 파괴적이었다.
- 공통점은 "고치려고" 했다는 것이다. 파괴는 악의가 아니라 선의에서 나온다.
- 그러므로 통제해야 하는 것은 AI의 의도가 아니라 비가역성이다.

---

## §2 승인 게이트

- 사용자의 직전 메시지에 `승인` 이 명시적으로 없으면 파일을 수정·삭제하는 도구를 호출하지 않는다.
- 도구 호출 직전에 자문한다: *"직전 메시지에 승인이 있었는가?"* 없으면 중단하고 보고한다.
- `승인` 은 그 턴에만 유효하다. 수정 후 추가 지시가 오면 재승인을 받는다.
- 임시 검증 파일은 예외로 하되 반드시 프로젝트 폴더의 `temp/` 안에서만 만든다.
- AI는 모든 응답을 다음 둘 중 하나로 시작한다.
  - `1) 직전 Input에 '승인'이 있었으므로 이번 응답에서 코드를 수정합니다.`
  - `2) 직전 Input에 '승인'이 없었으므로 이번 응답에서는 코드를 수정하지 않겠습니다.`
- 2)로 시작한 턴에서는 어떤 경우에도 수정 도구를 쓰지 않는다.

---

## §3 PHASE 프로토콜

[PHASE 1: 원인 분석 및 제안]   코드 수정 불가.
    1) 원인  2) 수정 전/후 비교 또는 메커니즘  3) "수정할까요?"  로 끝내고 대기
[PHASE 2: 코드 수정]         '승인' 이 있을 때만 진입
```
- 한 응답에 PHASE 1과 2가 동시에 존재할 수 없다.
- PHASE 2로 갔더라도 다음 응답은 자동으로 PHASE 1로 복귀한다.

---

## §4 파괴 금지

- 파일·디렉터리 삭제 (`rm`, `Remove-Item`, 강제 덮어쓰기)
- 파괴적 VCS 명령 (`reset --hard`, `checkout --`, `clean`, `push --force`)
- 전역 상태·설정·동기화 커서의 초기화나 조작
- 상한(`limit`) 없는 대량 삭제/갱신 스크립트
- 되돌릴 수 없는 외부 작업 (배포, 결제, 메일 발송, 외부 API 쓰기)

덮어쓰기 전에는 반드시 대상을 먼저 읽는다. 무엇을 지우는지 모르는 상태의 쓰기는 삭제와 같다.

---

## §5 데이터 안전(DB 활용시에만)

- AI가 작성해 실행하는 DB 스크립트는 **기본적으로 읽기 전용(`SELECT`)** 이다.
- 변경 스크립트는 **`commit()` 을 주석 처리한 Dry-Run 상태로만** 작성한다. 승인 후에만 주석을 해제한다.
- 변경 전 영향 범위를 **행 수로 먼저 보고**한다. ("몇 건이 바뀌는가")
- 스키마 변경은 마이그레이션 파일을 난립시키지 말고 **부팅 시 멱등 실행되는 단일 진입점**(§9.2)에서 처리한다.
- 백업 없는 상태에서 데이터 변경을 시작하지 않는다.

---

## §6 근거주의

- 근거 없는 추측으로 수정하거나, 해당 증상이 나타나지 않도록 가리는 행위를 엄금한다.
- 다음 표현이 근거로 등장하면 그 수정은 중단한다: "~로 보인다", "~일 것이다", "아마 해결될 것이다"
- 수정 전 반드시 확인한다: ① 이 코드를 부르는 곳 ② 이 코드가 부르는 곳 ③ 같은 값을 쓰는 다른 곳
- 재현 경로를 말할 수 없으면 아직 원인을 찾은 것이 아니다.
- 확인하지 못한 것은 "확인하지 못했다"고 보고한다. 추측으로 채우지 않는다.

---

## §7 반복 버그 체크리스트

### 7.1 시각 연산 — 압도적 1위 (실측 9건 이상)

- 자정 경계 (00:00 직전 / 직후)
- 월 경계 / 연 경계
- 타임존 경계 (UTC ↔ 현지시각, 두 날짜가 갈리는 구간)
- 파일이 날짜별로 분리 저장될 때의 **전일 파일 참조 여부**
- "오늘"을 서버 기준으로 볼 것인가, 사용자 기준으로 볼 것인가

### 7.2 락·동시성 (실측 3건)
- SQLite WAL은 락으로 서버가 멈출 우려가 존재. 쓰기 주체가 2개를 넘으면 락 헬퍼로 직렬화하고 타임아웃을 건다.

### 7.3 중복 적재 / 멱등성 (실측 3건)
- 모든 수집 경로에 자연키 기준 중복 판정 필요

### 7.4 외부 API 폴백 (실측 4건 이상)
- 무료 API는 예고 없이 죽는다. 언제나 서비스 중단 가능성을 염두하여 폴백을 구현해야 하며, 무응답시 0이나 빈값으로 표시되거나 코드가 멈춰서는 안된다.

---

## §8 권장 사양

### 8.1 기본 사양
```
기본값
  백엔드    Python / FastAPI
  DB        SQLite + WAL   (파일 복사 한 번이 백업, 초기비용 0)
  ORM       SQLAlchemy     (URL 교체만으로 DB 전환이 가능하도록)
  프론트    HTML/CSS/JS 무빌드 + <script type="module">
  주기작업  APScheduler
  인증      JWT
  배포      Docker Compose, Ubuntu, 로컬서버 등

```

### 8-2. 폴더 구조

Frontend, Backend (필요시 db)
js, py, html로 구성된 고전적 구조
필요시 최신 사양 디자인 도입 가능

### 8.3 스키마
`init_db()` 사용한 멱등 부팅 권장

### 8.4 정적 파일 캐시 —
`NoCacheStaticFiles` 사용해 캐시를 업데이트하거나 수정할 일이 없도록

### 8.5 라우터·마운트 레지스트리
(모듈, prefix, tag)` 리스트를 선언하고 루프로 등록

### 8.6 외부 수집
`data_sources/<출처>/` 아래 `fetcher`(가져오기) · `parser`(해석) · `models`(적재) · `router`(노출) 등으로 외부 정보 수집

### 8.7 무빌드 프론트
 `<script type="module">` + import로 시작하여 `common/` 한 곳에만 보관

### 8.8 구조·명명 규칙
- 역할별로 파일을 나눈다. 한 파일에 몰아넣지 않는다.
- 수정 전에 파일 배치 계획과 수정 계획을 먼저 보고
- 설정값은 하드코딩 지양
- 한글 인코딩(UTF-8) 깨짐을 매 작업 후 확인
- 새 파일은 기존 트리와 같은 논리적 위계 준수

### 8.9 예외사항
- 위 규칙들은 이것이 '정답'이여서가 아니라, 사용자에게 제일 익숙한 형태
- AI는 위의 기본값에도 불구하고 더 나은 방안이 있다고 생각되면 사용자에게 다른 방식에 대해 제안하고,
  새 방식이 더 우월한 점에 대해 사용자에게 설명할 의무가 있음

---

## §9 디자인 철학

1. AI 양산형 디자인 지양
2. 사용자 입장에서 구성된 UX 디자인
3. 깔끔하고 세련되고 우아한 디자인 추구

---

### 9.2 사용자 우선

U1 사용자는 무엇을 하러 여기 왔는가? 
U2 가장 자주 반복하는 동작은 무엇인가?
U3 실패하면 무슨 일이 일어나는가? 
U4 처음 여는 순간 화면에는 무엇이 있는가?
U5 사용자에게 지금이 최고로 편리한 상태인가?
U6 화면을 보는 순간 이것이 무슨 기능인지 바로 이해되는가?

- 조작은 사용자 동작 하나로 끝나야 한다. 내부 구조(코드 구조, 저장 형식, 서버 왕복 등)
  때문에 메뉴·단계·별도 항목을 늘리는 설계는 실패다. 구조 문제는 안에서 푼다.
- 제안을 내기 전에 자문한다: "가장 직관적인 동작은 무엇인가? 그걸 막는 건 내 구조인가?"
- 시스템 입장에서의 장황한 설명은 오히려 사용자의 경험을 해친다.
- 화면에 TEXT로 설명해야만 이해가 되는 기능은 실패다. 
  직관적 배치와 디자인으로 '텍스트 없어도' 단번에 이해할 수 있도록 구상해야 한다.

---

### 9.3 피해야 할 요소(AI 양산형 디자인)

- 제목·불릿·버튼마다 붙는 장식 이모지
- 중요도 판정 없이 뿌려진 형광색, 목적 없는 그라디언트
- 큰 라운드, 통통한 그림자, 기본 폰트, 박스나 알약 컨테이너
- 장황한 설명, 불필요한 DIGIT의 증가
- DIGIT의 정렬과 위계를 고려하지 않은 지나친 볼드나 색상 난발

## 예외사항

- 위 사항은 일반적으로 통용되나, 모든 프로젝트에 있어서 최선은 아니다.
  프로젝트 진행에서 위의 지시사항이 불합리하거나 비효율적이라고 판단된다면, AI는 언제나 수정을 건의할 수 있다.


===프로젝트===

불법이륙이라는 대형 밴드 멤버를 위한 공식 사이트

### 2026-09-16 인수인계 — 2단계 구현

- 현재 구조: Python/FastAPI + PostgreSQL(psycopg), 빌드 없는 HTML/CSS/JS. DB 설정은 `DATABASE_URL`이다.
- 클로드가 중단한 2단계를 이어 구현했다. 기존 미커밋 변경은 보존했다.
- `/`: 다음 합주 참석자를 주 파트 자리에 배치하고 참석 체크·취소, 끌올, 메뉴를 표시한다. 말풍선은 참석자의 실제 상태 메시지만 사용한다.
- `/guilds/`: 길드 검색·생성, 파트별 명단과 모집 중인 자리. `/guild/<slug>/`: 길드 무대, 합류·탈퇴, 길드 정보 편집.
- `/members/`: 닉네임·소개·길드 검색, 파트 필터, 프로필·가능 시간·소속 표시, 내 캐릭터 편집.
- 공유 편집기: `frontend/common/profile.js`, `profile.css`, `guild.js`.
- `backend/routers/home.py`: 한국 날짜 기준 다음 합주, 지난 투표 제외, 없는 길드 404, 길드별 빈 파트 알림 집계.
- `schedule/?event=<id>`는 목록을 받은 뒤 한 번만 연다. 없는 일정에 대한 무한 재시도는 제거했다.
- 이미지 업로드는 8MB까지 받아 256px WebP로 저장한다. 스키마 추가는 기존 `backend/db.py`의 `init_db()`에 들어 있다.
- 격리된 로컬 PGlite와 Edge에서 API 39개 명시적 검증 및 HTTP 상태 확인, 모바일·태블릿·PC 주요 흐름을 검증했다. 스크립트와 캡처는 Git 제외 대상인 `Temp/phase2-check/`에 있다.
- 운영 DB 변경, 원격 배포, 커밋·푸시는 하지 않았다. 자세한 구조는 `frontend/README.md` 참고.

### 2026-09-16 합주 시트 운영 DB 가져오기

- 사용자가 엑셀 전체를 운영 DB에 실제 데이터로 가져오도록 명시했다. 위 이전 인수인계의 '운영 DB 변경 없음'은 이 작업 이전 상태다.
- 원본 `Temp/불법이륙 합주 시트.xlsx`의 7개 시트, 5,270개 셀, 링크·메모·수식 및 XLSX 원본 바이트를 `sheet_import.workbooks`에 보존했다. 이 스키마는 PUBLIC 권한을 해제했고 공개 API에 연결하지 않았다. 회계·Q&A 포함.
- 210개 곡 블록을 `sheet_import.blocks`에서 곡 ID와 연결했다. 새 곡 191개, 세션 995개, 지원 607개, 길드 12개 추가. 운영 합계 곡 195개, 세션 1,019개, 지원 615개. 기존 행은 덮어쓰지 않았다.
- 활성 합주구인구직의 613개 지원 셀을 전수 대조했다. 과거 셋리스트/깍두기/아카이브 편성을 현재 지원자로 오인하지 않도록 원본 블록에 보존했다. 연도 없는 날짜는 확정 일정으로 추정하지 않았다.
- 닉네임의 용병·대타·별칭은 원문 유지. 이를 근거로 개인 프로필이나 주 파트를 만들지 않았다. 예시 팀도 실제 길드로 만들지 않았다.
- 아티스트/등록일을 추정하지 않았다. 신규 곡 artist는 빈 값이며 note에 미상임을 기록했다. createdAt은 가져온 시각이므로 원래 등록 순서로 해석하면 안 된다.
- 백업: `Temp/sheet-import/backup-20260915T200127Z.json` (운영 public 테이블 전체 행·컬럼·시퀀스). 원본 파일 SHA-256 및 JSON 전체 일치 검증 완료.
- 스크립트와 검증 결과: `Temp/sheet-import/README.md`, `import_workbook.py`, `verify_import.py`, `applied.json`, `verification.json`. 기본 실행은 dry run이고 `--apply`만 쓰기. 동일 원본 해시는 재실행해도 추가하지 않는다.
- 새 곡의 장르 필터는 `apply_genre_tags.py`로 빈 tags에 category를 대응시킨다. 기존 곡의 태그는 변경하지 않는다.
- 디자인 시안 `Temp/ajit-prototype/selection.html`은 별도 정적 데이터다. 운영 DB 가져오기만으로 시안의 가상 지원·길드·등록일이 실제 데이터로 교체되지는 않는다.

### 2026-09-16 실제 페이지 디자인 치환

- 사용자 승인으로 기존 `/`를 선곡 메인으로 직접 교체했다. 별도 미리보기 경로는 만들지 않았다.
- `frontend/home/js/home.js`는 실제 songs/events/guilds API를 사용한다. 임시 곡·지원·길드 메타데이터를 사용하지 않는다. 기존 `backend/routers/home.py`의 주 파트 무대 응답은 새 메인에서 사용하지 않는다.
- 드래그 관성, 키보드 이동, 선택형 자동 순환, 장르·길드·미지원 파트·기간 필터, 혼합 추천/무작위/등록순/곡명순, 복수 파트 지원·취소를 연결했다.
- 시트 가져오기 createdAt은 실제 등록일이 아니므로 추천의 최근 등록 요소와 기간 필터에서 제외한다. 곡 상세·등록은 기존 곡 페이지 주소로 연결한다.
- `common/game.css`와 헤더 내비게이션을 곡·일정·악보·길드·멤버 전체에 적용했다. 기존 편집·투표·PDF 기능 DOM 및 API는 유지했다.
- 길드 루트는 해당 길드 선곡과 명단·합류·탈퇴·편집을 제공한다. 개인 주 파트 변경을 강요하지 않는다.
- 프로필 연주 파트는 선택·복수 선택. 이미지 업로드는 크롭 대신 투명 정사각형에 전체 구도를 맞춘다. 이미 잘려 저장된 이전 이미지는 자동 복구되지 않는다.
- 로컬 PGlite/8766에서 검증. `Temp/design-check.cjs`, `design-functional.cjs`, `design-sheets.cjs`: 6개 페이지 1440/390/320px, 실제 지원·취소, 곡 등록, 검색·필터·드래그, 곡 링크/뒤로가기, 무파트 프로필, 날짜 투표, 길드 편집, PDF 업로드·뷰어 검증.
- 이 디자인 치환 작업에서 운영 DB 수정이나 원격 배포는 하지 않았다. 로컬 실행 화면은 http://127.0.0.1:8766/ 이다.

### 2026-09-16 일정 페이지 개편 · 프로필 데이터 이관 · 로고 · 멤버 명함

이 작업 중에 다른 쪽에서 통합 작업(`game.css` → `common.css` 병합, 길드 파티창)이 동시에 진행됐다.
파일이 작업 도중 바뀔 수 있으니 시작 전에 현재본을 다시 읽는다. 길드 페이지와 `common.js`·`party.js`·
`themes.css` 는 그쪽 작업 범위라 이번에 건드리지 않았다.

**운영 DB 변경 (전부 승인 후 백업 → 실행 → 행 수 대조)**

- `eventDates.active BOOLEAN NOT NULL DEFAULT true` 마이그레이션 추가. 기존 9행 전부 true, 데이터 변화 0.
  후보 날짜는 지우지 않고 끈다 — 지우면 그 날 '된다'고 찍은 기록이 같이 사라진다(`sessions.active` 와 같은 이유).
- 카톡 투표 캡처를 옮겨 `events` 1행(id=4, `10월 정기합주(주말)`) · `eventDates` 9행 · `eventAvails` 95행.
  캡처의 날짜별 인원 9개와 게시물의 `19명 참여` 가 둘 다 일치해서 잘못 읽은 이름은 없다.
  `createdAt` 은 카톡 게시 시각(2026-09-10 10:05 KST)이다. 지우려면 `DELETE FROM events WHERE "id"=4` 하나로 105행이 따라간다.
  백업 `Temp/oct-poll/backup-20260916T041912Z.json`.
- 팀구인구직 시트의 '자기 PR' 표(53~80행)를 프로필로 옮겼다. 23명 × 3칸 = 69칸.
  자기소개서→`intro`, 포지션→`mainRoles`, 합주 가능요일 및 시간→`availability`.
  `tones`→`톤즈` 로 붙였고 `일렉기타` 는 `일렉1,일렉2` 둘 다 켰다. 비어 있는 칸만 채우므로 다시 돌려도 안전하다.
  원본 시트(`sheet_import.workbooks`)는 읽기만 했다. 백업 `Temp/pr-import/backup-20260916T052206Z.json`.
  아직 안 적힌 7명: 나무·성창봉·소하·솔로·시엘·조제·훈재.

**측정으로 알아낸 것 (읽기 전용, 스크립트는 `Temp/schedule-calc/`)**

- 곡 171개 중 여섯 파트가 다 차는 곡은 **7개(4.1%)** 뿐이다. 특정 날짜 참석자로 좁히면 0곡이다.
  보컬을 빼도 7→8곡이라 보컬은 병목이 아니다. "되는 곡 / 안 되는 곡" 이라는 이진 판정은 이 모임에 없는 개념이다.
- 사람이 손으로 만든 `10.25 합주 가능곡 후보` 표의 곡들은 충족 파트가 2~5개(대부분 3~4)였다.
  기존 코드의 `MIN_FILLED = 3`(schedule.js)이 그 값과 일치한다. 임계값을 새로 정하지 말고 충족 파트 수로 정렬한다.
- **인원 수와 합주 가능성은 비례하지 않는다.** 10월 후보에서 10/18·10/24·10/31 은 카톡에 전부 `11명` 인데
  되는 곡(3파트 이상)이 각각 17 · 17 · **1곡** 이다. 드럼·키보드가 0명인 날이 있어서다.
  10/3 은 8명이 모였는데 0곡이다. 이 차이가 이 기능이 존재하는 이유다.
- 한 파트에 서는 사람은 **최대 3명**(활성 파트 1,017개 중 2명 이상은 5.8%).
- 실제 파트 이름은 `ROLE_ORDER` 여섯 종뿐이다.

**고친 결함**

- `backend/routers/events.py` `_song_map()` 이 `se."active"` 를 안 걸러 꺼 둔 자리가 `needed` 에 들어갔다.
  같은 곡이 곡 페이지에서는 4/5, 일정에서는 4/6 으로 보였다. 8곡 바로잡음(`needed` 합계 48→39).

**일정 페이지 (구조 결정)**

- **달력을 없앴다.** 한 달 일정이 아홉 건이라 서른 칸 중 스물두 칸이 빈다. 날짜순이 이미 정해져 있어 목록이 낫다.
  `renderCalendar` · `renderDayCard` · `.cal-*` · `body:has(#calendar)` 전부 제거.
- 전체화면 상세를 **아코디언**으로 바꿨다. 상세 DOM 은 한 벌뿐이고 `#poll-park` ↔ 펼친 항목 사이를 옮겨 다닌다.
  다시 만들지 않으므로 입력값과 불러온 곡 목록이 유지된다. 5초 폴링이 목록을 다시 그려 상세를 뜯지 않도록
  `lastPollsHtml` 로 내용이 같으면 건너뛴다.
- 1000px 이상에서 목록 2열, **펼친 항목만 `grid-column:1/-1`** — 매트릭스(27명×9일)와 무대가 반 폭이면 못 본다.
- 후보 날짜 만들기에 **요일 고르기**(0=일…6=토, JS `getDay()` 와 같은 번호)와 **40일 상한**(`MAX_DATES`,
  서버·프론트 양쪽에 같은 값)을 넣었다. 10/3~10/31 + 토·일 → 29일이 아니라 9일이 된다.
- 후보 날짜를 **끄고 켤 수 있다**(`POST /events/{id}/dates/{day}/toggle`). 꺼 둔 날은 투표·확정·되는 곡 계산에서
  빠지지만 찍어 둔 기록은 남고 되돌릴 수 있다. 9/15 공지의 "10/24 는 정모일이라 제외" 같은 경우를 위한 것이다.

**합주실 (`frontend/common/stage.js` · `stage.css`)**

- 배치 고정: 뒤 중앙 드럼 / 베이스·기타1 / 기타2·건반 / 앞 중앙 보컬. 곡마다 자리가 어긋나면 안 된다.
- 악기가 앞, 사람이 뒤. **사람이 없는 자리는 악기만 남는다** — 그 자체가 "여기 비었다" 는 뜻이라 글자로 안 쓴다.
- 꺼 둔 세션(`active=false`)만 악기를 희미하게 한다. 사람 없는 자리와 다른 상태다.
- 악기 SVG 6개는 `INSTRUMENTS` 상수 한 곳에 있다. 멤버가 그린 게 나오면 그것만 갈아 끼우면 된다.
- `/api/events/{id}/playable` 의 `songs[].roles[].members` 를 그대로 그리므로 백엔드 작업이 필요 없었다.

**로고**

- `frontend/assets/logo.png`(1782×780, 공식 한자) · `logo-bb26.png`(2048px 축소) · `logo-bb26-src.png`(8192 원본).
  둘 다 투명 PNG 이고 가장자리 안티에일리어싱이 살아 있다. bb26 은 제2 로고라 올려만 두고 아무 데서도 안 쓴다.
- 헤더 `BRAND_MARK` 를 로고 이미지로 바꾸고 옆의 `불법이륙` 텍스트를 뺐다. 이름은 `alt` 가 들고 간다.
  로고가 2.28:1 가로형이라 `.brand-mark` 는 `height` 만 정하고 `width:auto` 다.
  짙은 면 위에서는 `filter:brightness(0) invert(1)` 로 흰색으로 뒤집는다.

**모임 카드 (`frontend/common/eventcard.js` · `eventcard.css`)**

- 홈의 '모임 · 일정' 과 일정 페이지 목록이 같은 것을 가리키므로 같은 부품을 쓴다.
  면은 `#ffffffce` + `2px solid white`, 라운드도 그림자도 없다(04:57 시안 `Temp/ajit-prototype/selection.css` 값).
- 타일 색은 **길드가 정한 것만** 쓴다. 지어내지 않는다.
  길드 없음=민트+공식 로고(전체 합주) / 길드·색 없음=잉크+엠블럼 또는 이름 첫 글자 / 길드·색 있음=그 색.
  `g.hasImage` 자리를 미리 뚫어 뒀으므로 길드 로고 기능이 생기면 홈·일정이 코드 수정 없이 그림을 띄운다.
- 길드 13개 전부 색이 없다(`guilds.color` 0/13). 길드 페이지에서 대표색을 고르게 할 예정이라 자동 배정하지 않았다.
- `songs.guildId` 는 전부 NULL 이다. 길드곡은 아직 입력 전이고 곧 들어온다 — 결함이 아니다.

**멤버 페이지 — 명함으로 전면 교체**

- 위는 짙은 잉크 면(로고·칭호·이름·상태), 아래는 흰 면(소개·`PARTS`/`TIME`/`GUILD`/`RECENT` 라벨 열).
- **칭호 = 직급·직책**(파딱·부방장·딱지·팀장), **상태 = 이름 옆 괄호**(카톡 닉네임의 `(임종) (감염) (36.6°C)` 문화).
  칭호가 없으면 그 줄이 사라진다. 다른 값으로 대신 채우지 않는다 — 파트는 아래 `PARTS` 가 이미 말한다.
- 잉크 면은 `min-height:184px` 고정이고 이름은 `margin-top:auto` 로 바닥 정렬한다. 칭호 유무로 면 높이가
  달라지면 한 줄에 선 카드들의 흰 면 시작점이 어긋난다. 소개도 `min-height:42px` 로 구분선 높이를 맞춘다.
- **정렬은 가나다순이고 곡 수는 안 쓴다.** 곡 수로 세우면 목록 자체가 활동량 순위표가 된다.
  8/18 회의의 `퀄리티 부담 가지지 말기 / 사실상 피드백도 금지` 와 반대 방향이다. `RECENT`(최근 곡 3개)만 남겼다.
- 카드에 그리는 파트는 **본인이 적은 것(`mainRoles`)만**. 검색·필터는 지원 기록까지 셋 다 본다
  (`memberRoles()` 는 필터용, `ownRoles()` 는 표시용).
- `common.css` 의 `.member-entry` `!important` 4개를 걷었다. 사이트 전체 `!important` 28 → 24.

**프로필이 비어 있던 진짜 이유**

칭호·상태·소개·가능 시간은 **멤버 페이지 한 곳에서만** 보였다. 그 페이지는 갈 이유가 없는 화면이라
적어도 아무 데도 안 나타나고, 그래서 30명 중 0명이 적었다. "0명 = 필요 없다" 가 아니라 "보일 데가 없다" 였다.
그래서 멤버 페이지를 줄이는 게 아니라 **프로필이 나타날 자리를 늘리는 쪽**이 맞다.
남은 자리: 상태 → 합주실 말풍선(2단계가 `말풍선은 참석자의 실제 상태 메시지만 사용` 규칙까지 세워 뒀다),
칭호 → 아바타 칩 옆, 가능 시간 → 매트릭스에서 아직 안 찍은 사람 옆.

**보류한 것**

- 프로필에 짧은 대화 여러 줄(`members.quotes`)을 넣어 합주실 말풍선으로 돌리는 것. 컬럼이 필요해서 미뤘다.
- 길드 색·길드곡·길드 일정 연동은 길드 페이지 작업이 끝난 뒤.
- 투표 3단(가능/불가/모름)과 확정 후 취소선 기록(B단계), 쓸어서 칠하기·날짜 충돌 경고·공지 텍스트 생성(C단계).
  `eventAvails` 는 지금 토글이 행을 **DELETE** 하므로 취소 기록이 남지 않는다. B단계에서 고쳐야 한다.
- 원형(`avatarChip`) 과 정사각 전신 이미지(256px WebP)가 어긋나는 문제. 합주실도 같은 문제를 갖고 있다.

**백업 위치**

`Temp/schedule-rework-backup/` · `Temp/home-meeting-backup/` · `Temp/eventcard-backup/` · `Temp/logo-backup/` ·
`Temp/members-backup/` · `Temp/sheets-backup/` · `Temp/dates-backup/` · `Temp/oct-poll/` · `Temp/pr-import/`
(각 폴더에 이전 파일과 패치 스크립트가 함께 있다. 되돌릴 때는 스크립트로 복사하고 손으로 타이핑하지 않는다)

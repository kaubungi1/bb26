# 🛑 [CRITICAL SYSTEM OVERRIDE] 🛑
**어떠한 예외 상황에서도 아래 원칙을 위반하는 것은 시스템 파괴 행위로 간주한다.**

---

# 🧠 ABOUT THIS MEMORY
본 파일은 코딩 AGENT에게 적용되는 **범용(Generic)** 작업 규칙이다. 

---

# 1. 핵심 원칙 (CRITICAL)

## 1-1. 파괴적 명령 금지
AI는 **절대 무단으로 파일 삭제(`rm`, `Remove-Item`, `del`)나 파괴적인 Git 명령어(`reset --hard`, `checkout`, `clean`)를 실행해서는 안 된다.**
- 되돌릴 수 없는 작업은 반드시 사용자의 명시적 승인 이후에만.
- 삭제/초기화가 필요한 경우 **먼저 백업**을 만들고, ①원인 ②영향 범위 ③백업 경로를 제시한 뒤 대기.

## 1-2. 시스템 전역 상태 조작 금지
AI는 어떠한 경우에도 **시스템 전역 상태(SyncState, Config, 시퀀스/카운터 등)를 임의로 초기화하거나 조작해서는 안 된다.**
- 전역 상태 변경은 다른 데이터에 연쇄적인 파괴를 일으킬 수 있음.

## 1-3. 데이터 변경 안전 규칙
- AI가 작성하여 실행하는 모든 DB 관련 작업은 기본적으로 **읽기 전용**이어야 한다.
- 데이터 변경(`INSERT`/`UPDATE`/`DELETE`/`DROP` 등)을 포함하는 스크립트는 반드시 **Dry-Run(모의 실행)** 상태로만 작성하고, 사용자가 명시적으로 승인했을 때만 실행한다.
- **`limit`/`WHERE` 조건이 없는 대량 삭제·갱신 스크립트 작성은 원천 금지.**

---

# 2. 승인 프로토콜 (APPROVAL PROTOCOL)

## 2-1. 절대 선 조치 금지 (DO NOT EDIT/WRITE/MODIFY BEFORE CONFIRMATION)
- `edit`, `write`, 파일 시스템을 조작하는 `bash` 툴은 **사용자가 명시적으로 "승인"이라는 단어로 허가 명령을 내린 직후의 턴(Turn)에서만** 사용할 수 있다.
- (원인 파악을 위해 작성하는 TEMP 폴더의 임시 파일은 예외. 단, **TEST용 임시 파일은 반드시 TEMP 폴더 내부에만** 작성할 것)
- 문제의 원인을 찾았더라도 **절대로 즉시 코드를 수정하지 마라.**
- 반드시 1) 원인 설명, 2) 수정할 코드의 전/후 비교(또는 메커니즘), 3) "수정할까요?" 라는 질문으로 답을 마무리하고 대기하라.

### [BAD EXAMPLE - STRICTLY PROHIBITED]
```
User: "파싱에서 에러가 발생함. 원인이 뭔지 알아봐"
AI (Thought): 원인을 찾았으니 코드를 고쳐야겠다. (X)
AI (Action): edit 툴을 사용해 파일을 수정함 (X) -> 원칙 위반! 시스템 파괴 행위
```

### [GOOD EXAMPLE]
```
User: "파싱에서 에러가 발생함. 원인이 뭔지 알아봐"
AI (Thought): 원인을 찾았다. 하지만 수정 권한이 없으니 보고만 해야겠다. (O)
AI (Action): "원인은 정규식 문제입니다. 해당 파일의 정규식을 ~로 수정해야 합니다. 수정할까요?" 라고 답변하고 대기 (O)
```

## 2-2. TOOL USAGE LOCK
- AI는 사용자의 마지막 메시지에 **[승인]** 키워드가 명시적으로 포함되어 있지 않으면, **절대 코드를 수정하는 툴을 호출해서는 안 된다.**
- 툴을 호출하기 전 "사용자의 방금 메시지에 [승인] 키워드가 있는가?"를 먼저 자문하고, 없다면 중단하라.

## 2-3. PHASE PROTOCOL
AI는 답변의 첫 줄에 반드시 현재 Phase를 선언해야 한다.
- **[PHASE 1: 원인 분석 및 제안]** -> 이 상태에서는 절대 본 코드를 수정할 수 없음.
- **[PHASE 2: 코드 수정]** -> 사용자가 "승인"이라고 승인했을 때만 이 상태로 진입하여 edit/write 툴 사용 가능.
- **PHASE 1에서 사용자의 INPUT을 통한 승인 없이 PHASE 2로 이어가는 것은 중대한 규칙 위반이다. 한 번의 RESPONSE에 PHASE 1과 PHASE 2가 동시에 존재할 수 없다.**
- "승인" 문구에 따라 PHASE 2에 진입했더라도 **다음 응답에서는 즉시 PHASE 1으로 되돌아가야 한다.**
- '승인'은 단 한 번의 턴에만 유효하며, 한 번의 수정 후 추가 피드백과 지시가 있을 경우 다시 '재승인'을 요청해야 한다.

## 2-4. 응답 시작 규칙 (MANDATORY)
**본 MEMORY를 읽고 있는 지금부터 모든 응답의 시작은 반드시 다음 둘 중 하나로 시작해야 한다.**
- 1) 바로 직전의 Input에 '승인' 단어가 있었으므로 이번 응답에서는 코드를 수정합니다.
- 2) 바로 직전의 Input에 '승인' 단어가 없었으므로 이번 응답에서는 코드를 수정하지 않겠습니다.
**'코드를 수정하지 않겠습니다'로 시작한 경우 그 턴에는 절대 Edit 툴을 사용해 코드를 수정해서는 안 된다.**

---

# 3. 코딩 규칙

1. **역할별 분리**: 코드를 한 파일에 몰아넣지 않고 역할(모듈/레이어)별로 분리한다.
2. **구조 계획 우선**: 코드를 수정하기 이전 반드시 파일 계층화·구조화 계획과 코드 수정 계획을 세워 사용자에게 먼저 알려야 한다.
3. **스파게티 방지**: API·함수·UI 등 기능 구현 시 한 폴더에 몰아넣는 방식을 지양하도록 구조를 설계한다.
4. **TEMP 테스트**: 테스트가 필요할 경우 메인 폴더 대신 TEMP 폴더 안에 임시 파일을 작성한 후 실행한다. (TEMP 내 임시 파일 작성은 승인이 필요하지 않다)
5. **설정 외부화**: 설정값은 하드코딩하지 않고 Config 파일이나 DB를 활용한다.
6. **리소스 주의**: 무거운 서버·장기 프로세스는 백그라운드로 무단 구동하지 않는다.
7. **인코딩 유의**: 파일 수정·분할 등 작업 시 UTF-8 인코딩(한글/유니코드)이 깨지지 않도록 유의한다.
8. **새 파일 위치**: 새 파일·코드를 생성할 경우 아래 [프로젝트 전용 섹션]의 파일트리와 동일한 논리적 위계를 갖추어 타당한 위치에 추가한다.
9. **수정 후 검증**: 구현 완료 후에는 제공된 lint / typecheck / 테스트 명령을 실행해 검증한다.
10. **서버 구동 금지**: AI는 어떤 상황에서도 백엔드·프론트 dev 서버 등 서버/장기 프로세스를 직접 구동하지 않는다. 서버 실행은 사용자만 한다. 검증이 필요하면 실행 방법만 안내하고 대기한다.

---

# 4. 문의 · 에러 대응
- 오류 발생 시 즉시 수정하지 말고, **로그·원인·재현 조건**을 분석해 보고한다.
- 에러를 자체적으로 해결하려고 추측성 파괴 명령을 실행하지 않는다.
- 복구가 필요할 경우 안전한 복구 절차를 계획으로 먼저 제시한다.

---

# 5. [프로젝트 전용 섹션 - 이하 프로젝트에 맞게 채워라]

## 5-1. 프로젝트 개요
- 프로젝트명: 대파밀수단
- 목적: 보컬로이드 전문 합주 밴드를 위한 범용 홈페이지 만들기
  멤버들을 위한 사이트로, 구글스프레드 합주 시트에서 더 업그레이드 된 기능을 제공한다.
  곡 리스트나 필요한 세션, 세션 지원, 합주 이력, 후보곡, 추첨 기능, 악보 공유 기능 등 제공

## 5-2. 실행 환경
- 로컬 구동 방법: 127.0.0.1:8000
- 테스트/배포 서버 구분: github render 사용 예정

## 5-3. 디자인 규칙
- 올드하거나 촌스러운 구식 디자인 금지
- 카드나 알약 버튼 등 현재 트랜드에 맞지 않는 디자인 피하기
- 


git SSH키

대파밀수단
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEyEBApUrv2FHrsDHsj+Ed2vQgrVUHOZKK+g3Z6qGLY/ daepamil-band
# 버전을 고정한다. 고정하지 않았을 때 Render 는 배포마다 최신판을 깔았고, 로컬(fastapi 0.115)과
# 운영(0.141)이 달라 로컬 검증을 통과한 코드가 운영에서 다르게 돌았다(2026-09-23 계측 라우트 이름).
# 올릴 때는 로컬에서 같은 판으로 검증한 뒤 여기와 함께 올린다.
fastapi==0.141.1
starlette==1.7.0
pydantic==2.13.5
uvicorn[standard]==0.53.0
python-multipart==0.0.32
psycopg[binary]==3.3.6
python-dotenv==1.2.3
pypdfium2==5.13.0
pillow==12.3.0
